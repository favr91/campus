<%= form_for(area) do |f| %>
  <% if area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
      <% area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="container" style="margin-top: 60px;">  
      <div class="row">
<div class="col-xs-12 col-sm-6 col-md-4 col-sm-offset-3 col-md-offset-4">

<div class="panel panel-default devise-bs">
  <div class="panel-heading">
    <h4>Registro de Area</h4>
  </div>
  <div class="panel-body">
   
      <div class="form-group">
        <label for="area.nombre">Nombre</label>
        <input autofocus="autofocus" class="form-control" type="text" name="area[nombre]">
      </div>
       
      <div class="form-group">
        <label for="area.descripcion">Descripcion </label>        
        <textarea rows="4" class="form-control" name="area[descripcion]"></textarea>        
      </div>
     
      
      <div class="actions">
    <%= f.submit  t('.Registar_Area', default: 'Registrar_Area'), class: 'btn btn-success btn-block'%>
   
  </div>
 <% end %>
</form>  </div>
</div>



</div>
</div>

  </div>
